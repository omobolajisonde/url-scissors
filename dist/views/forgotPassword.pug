extends base
block content
    section.signin
        form.form__sign(id="forgotPassword") 
            h1 Forgot Password
            div
                label(for="email") Email
                input(type="email", id="email", name="email", required)
            div
                button(type="submit", class="btn") Continue